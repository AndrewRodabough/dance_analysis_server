# =============================================================================
# Dance Analysis Server - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# cp .env.example .env

# -----------------------------------------------------------------------------
# Cloudflare Tunnel Configuration
# -----------------------------------------------------------------------------
# Get your tunnel token from Cloudflare Zero Trust Dashboard:
# 1. Go to https://one.dash.cloudflare.com/
# 2. Navigate to: Zero Trust → Networks → Tunnels
# 3. Create a tunnel named "dance-api"
# 4. Copy the token from the Docker installation command
CLOUDFLARE_TUNNEL_TOKEN=

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins for client applications
# Include your production domain and any development URLs
# Example: https://api.yourdomain.com,https://app.yourdomain.com,http://localhost:3000
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Set to true to use mock pose estimation (no GPU required, for testing)
USE_MOCK_ANALYSIS=false

# -----------------------------------------------------------------------------
# Storage Configuration (Optional - defaults are for local MinIO)
# -----------------------------------------------------------------------------
# For AWS S3, update these values:
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_ACCESS_KEY=your-aws-access-key
# S3_SECRET_KEY=your-aws-secret-key
# S3_BUCKET=your-s3-bucket-name

# For local development, these are already set in docker-compose.yml:
# S3_ENDPOINT=http://minio:9000
# S3_ACCESS_KEY=minioadmin
# S3_SECRET_KEY=minioadmin
# S3_BUCKET=dance-videos
